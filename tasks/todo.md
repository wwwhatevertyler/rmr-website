# RMR Website — To Do

## Current

- [ ] Configure Netlify form-submission email notifications in the Netlify dashboard
- [x] Replace homepage sponsor module with 21st-style ad gallery
- [x] Polish hero floating image interaction and unify click audio behavior
- [x] Add Netlify build output and clean route configuration
- [x] Wire advisor, contact, and newsletter forms to Netlify Forms
- [x] Keep blog static with clean `/insights/:slug` production routes
- [x] Exclude docs, tasks, stale exports, sandbox files, and audio experiments from production output
- [x] Connect Netlify project to GitHub and verify preview deployment
- [x] Add seamless same-tab navigation so background audio continues across internal pages
- [x] Add root README/DESIGN.md and archive unused source assets outside production paths
- [x] Refresh styleguide to match current homepage, blog, forms, metadata, footer, and motion rules
- [x] Move active website source into organized `website/` folders while preserving Netlify output paths
- [x] Update homepage social metadata and verify generated output before production push
- [x] Integrate `rmr-logo-new.png` as the primary site logo while preserving the original logo variant
- [x] Add homepage-only logo press/spin before the soft-navigation curtain sweep
- [x] Fix sampled click audio after logo-triggered soft navigation
- [x] Point GoDaddy DNS from Framer to Netlify after preview approval
- [x] Add production sitemap and robots file for Search Console
- [x] Generate static article pages with first-response SEO metadata
- [x] Add production launch handoff documentation
- [x] Polish mobile menu CTA styling and advisor arrows
- [x] Raise page transition curtain above persistent audio and cursor overlays
- [x] Point Netlify AJAX form submissions at their owning page routes
- [x] Polish contact form reason field for multi-select Netlify submissions
- [x] Clean up contact form Netlify email fields
- [x] Restore contact form backend reason field key
- [x] Align contact form backend field order and labels
- [x] Polish advisor form success state
- [x] Simplify mobile audio control to play/mute only
- [x] Remove mobile sampled click audio delay
- [x] Protect mobile background audio during tap cues and logo transitions

## Recent Verification

- 2026-07-04: Ran `npm run build`; `dist/` includes both `rmr-logo-new.png` and `rmr-logo.png`, and generated nav/footer references use `rmr-logo-new.png`.
- 2026-07-05: Ran `npm run build`; served `dist/` locally and verified via Brave headless CDP that the home logo press/spin finishes before the curtain starts, reduced motion skips the spin, normal home nav links transition immediately, and non-home logos do not get the spin hook.
- 2026-07-05: Rebuilt after switching the homepage logo experiment to `rmr-logo.png` and refining the spin easing; Brave headless CDP verified the revised animation ends before the curtain starts and reduced motion still skips the spin.
- 2026-07-05: Rebuilt after moving the homepage logo spin to WAAPI and increasing the opt-in logo to 44px; Brave headless CDP verified the logo stays inside the 68px nav, the 540ms animation finishes before the curtain, reduced motion skips it, and non-home logos remain unchanged.
- 2026-07-05: Rebuilt after rolling the 44px `rmr-logo.png` WAAPI click transition to all nav/footer logos; Brave headless CDP verified every active page nav/footer logo has the hook, representative nav/footer clicks finish animation before the curtain, and reduced motion skips the spin.
- 2026-07-06: Ran `node --check website/scripts/audio.js website/scripts/router.js` and `npm run build`; Brave headless CDP verified sampled click audio fires once before/after home and contact logo soft-navigation, repeated `RMRAudio.ensureMounted()` calls do not duplicate click sounds, the logo spring still precedes the curtain, and reduced motion skips the logo animation.
- 2026-07-06: Updated public SEO/social URLs to `https://www.rollmyretirement.com`; ran `node --check website/scripts/audio.js website/scripts/router.js`, `npm run build`, static headless Brave checks for home/advisors/contact/insights, and an in-process Netlify-style route check for `/insights/the-quiet-power-of-moving-your-401k`.
- 2026-07-06: Cut over GoDaddy DNS from Framer to Netlify. Public DNS checks via Cloudflare and Google resolve `www.rollmyretirement.com` to `rollmyretirement.netlify.app` and `rollmyretirement.com` to `75.2.60.5`; forced Netlify edge checks return `200` for home/advisors/contact/insights/article routes and `301` from the apex to `https://www.rollmyretirement.com/`.
- 2026-07-06: Added generated `sitemap.xml` and `robots.txt`; ran `node --check scripts/build-static.mjs` and `npm run build`, then verified the sitemap contains the homepage, advisors, contact, insights, and all eight article URLs on `https://www.rollmyretirement.com`.
- 2026-07-06: Added generated static article pages served at `/insights/<slug>` via generated `.html` files, with server-rendered title, description, canonical, Open Graph, Twitter, Article JSON-LD, embedded post data, and visible article body HTML. Verified `node --check scripts/build-static.mjs`, `npm run build`, `xmllint --noout dist/sitemap.xml`, Netlify article routing rules, and representative generated article metadata for `/insights/when-to-roll-over-your-401k`.
- 2026-07-06: Verified the deployed article route through Netlify and the custom domain: `/insights/when-to-roll-over-your-401k` returns `200` with static title/canonical/Open Graph/JSON-LD/H1 in the first HTML response, while the trailing-slash variant returns `301` back to the canonical no-slash URL.
- 2026-07-06: Added `docs/project/launch-2026-07-06.md` to package the launch, DNS cutover, Search Console, SEO improvements, verification, commits, and follow-up production checklist for future context.
- 2026-07-07: Fixed mobile menu CTA inheritance so mobile `Let's Talk` and `Apply to Join` buttons render as centered black pill CTAs with white text and arrows; added mobile `For Advisors →` decorative arrow spans. Ran `npm run build`, `node --check website/scripts/router.js`, `node --check website/scripts/audio.js`, inspected generated output, and verified mobile screenshots/computed styles for home/advisors/contact/insights/article routes.
- 2026-07-07: Raised `#page-curtain` to `z-index: 1000000` across production page templates so the black route-transition sweep covers persistent fixed overlays. Ran `npm run build`, inspected generated `dist` output for the updated curtain layer, and used headless Brave against a local `dist` server to verify soft-navigation transitions through home, advisors, contact, insights, and one article route keep the visible sweep above audio (`z-index: 9990`) and cursor (`z-index: 99999`).
- 2026-07-07: Verified live contact/advisor pages contain Netlify form markup, but live POST tests returned Netlify `404` before dashboard form detection was enabled/confirmed. Updated contact, advisor, and newsletter forms to include explicit `action` routes (`/contact`, `/advisors`, `/insights`) and changed AJAX submits to post to each form's own action. Ran `npm run build` and inspected generated `dist` output for the corrected form actions and submit targets.
- 2026-07-07: Pushed the corrected form action routes to production and verified the live HTML includes `action="/contact"` and `action="/advisors"`. Follow-up live POST tests still returned Netlify `404`, so Netlify Forms detection/capture must be enabled or confirmed in the Netlify dashboard before submissions will be stored.
- 2026-07-07: After Netlify Forms detection was enabled, pushed empty commit `288854d` to trigger a fresh Netlify form scan. Live POST tests to `/contact` (`consumer-contact`) and `/advisors` (`advisor-application`) returned `200`, confirming Netlify Forms capture is active for both lead forms. Email notification setup remains a dashboard configuration step.
- 2026-07-07: Converted the contact form intent pills from single-select `situation` radios to multi-select `reason` checkboxes with readable submitted values. Ran `npm run build`, inspected `dist/rmr-contact.html` for the new field and absence of old tokens, verified desktop/mobile pill layout plus multi-value `FormData` behavior in headless Brave, confirmed the live contact page served the new markup, and posted a safe Netlify contact test with two `reason` values that returned `200`.
- 2026-07-07: Cleaned up contact form Netlify email fields by replacing repeated submitted reason checkboxes with one hidden `reasons` aggregate and one optional `call_link` field. Ran `npm run build`, inspected `dist/rmr-contact.html` for hidden fields and absence of repeated `name="reason"`, verified the built mobile form POST body in headless Brave produces `reasons=Starting a rollover, Have old 401(k)s`, normalized `phone=+1 817-692-5262`, and `call_link=tel:+18176925262` with no bracketed array output, confirmed production served the new markup, and posted a safe live Netlify test that returned `200`.
- 2026-07-07: Restored the contact form backend field key from hidden `reasons` to hidden singular `reason` while keeping visible reason checkboxes UI-only. Ran `npm run build`, inspected `dist/rmr-contact.html` for exactly one submitted `name="reason"` field, verified the built mobile form POST body in headless Brave submits one aggregate `reason=Starting a rollover, Have old 401(k)s` value with no bracketed array output, confirmed production served the singular hidden `reason` field, and posted a safe live Netlify test with three aggregated reasons that returned `200`.
- 2026-07-07: Aligned the contact form backend payload order and labels with the visible form structure. Removed the separate `call_link` backend row, moved the hidden aggregate `reason` field into the Reasons section, changed visible copy to `City/State`, `Reasons for Reaching Out`, and `Anything else you'd like us to know?`, ran `npm run build`, verified the built mobile POST body order in headless Brave (`full_name`, `email`, `phone`, `location`, `reason`, `message`), confirmed production served the revised markup, and posted a safe live Netlify test with three aggregated reasons that returned `200`.
- 2026-07-07: Replaced the advisor form's green `Application Submitted` button state with the contact-style full success state and unified pending submit copy to `Sending...`. Ran `npm run build`, inspected generated advisor output to confirm the old success text was removed, verified the built advisor form in headless Brave with a stubbed POST, confirmed production served the new success block and `Sending...` copy, and posted a safe live Netlify advisor test that returned `200`.
- 2026-07-07: Simplified the mobile audio control to play/mute only by hiding the volume panel on coarse-pointer/mobile viewports while preserving the desktop hover slider. Ran `node --check website/scripts/audio.js`, `npm run build`, verified generated `dist/audio.js`, used headless Brave to confirm desktop keeps the visible hover slider (`display: flex`) while mobile hides it (`display: none`) with a 44px toggle, confirmed mobile tap toggles active state on/off, and verified soft navigation keeps a single persistent audio control.
- 2026-07-07: Removed mobile sampled click audio delay by adding a coarse-pointer pointer/touch release path that plays the cue before the follow-up mobile `click`, suppresses duplicate click sounds, cancels on scroll-like movement, and uses a decoded Web Audio buffer on mobile when available with the existing HTML audio pool as fallback. Ran `node --check website/scripts/audio.js`, `npm run build`, inspected generated `dist/audio.js`, verified mobile fallback and Web Audio cue paths in headless Brave, confirmed desktop still fires only on normal click, confirmed remounting does not duplicate listeners, and verified soft navigation keeps one audio control with mobile click cues still working after route swap.
- 2026-07-07: Made mobile sampled click cues Web Audio-only so iOS/coarse-pointer taps do not use the HTML audio fallback that can interrupt background music. Logo transition taps now suppress the generic mobile click cue so the spin animation has priority. Ran `node --check website/scripts/audio.js`, `node --check website/scripts/router.js`, `npm run build`, verified mobile logo navigation from contact to home stays soft (`beforeunload: 0`), fires one logo animation, keeps one audio control, and does not call/pause the click-sample HTML audio; verified normal mobile form taps start the Web Audio cue path with no HTML click playback; verified desktop still shows the volume panel and uses the existing HTML click sample path.
- 2026-07-08: Replaced the homepage sponsor ledger with a 21st-style six-panel ad gallery after FAQ and before Waves. Ran `npm run build`; verified generated home outputs place `#sponsor-ledger` after `#faq` and before `#waves`, copy local placeholder images from `dist/Website Images/`, contain no 21st.dev or Unsplash dependencies, keep placeholder logo panels `aria-hidden`, and pass headless Brave CDP checks at 1280/820/390px with 0 horizontal overflow, desktop hover expansion, tablet two-column layout, mobile one-column layout, reduced-motion static panels, no runtime exceptions, and FAQ accordion behavior still working.
- 2026-07-08: Smoothed the sponsor ad gallery hover handoff by moving the visible image treatment into inner surfaces while keeping the outer panels as gapless hover hitboxes. Ran `npm run build`; headless Brave CDP verified the generated homepage keeps `#sponsor-ledger` after FAQ and before Waves, desktop flex gap is `0px`, the visible gutter remains 8px, adjacent panel hitboxes touch with no no-hover reset, hover hands off smoothly from one panel to the next, placeholder logos stay unclipped, tablet/mobile remain static responsive grids, reduced motion keeps widths static, FAQ still opens, and no runtime errors fire.
- 2026-07-08: Restyled the homepage sponsor section into a Process-style editorial stack: dot marker, eyebrow, large serif heading, lead copy, full-width ad gallery, then the disclosure note underneath. Ran `npm run build`; headless Brave CDP verified copy sits above the gallery, disclosure sits below it, the gallery spans the full available content width, desktop hover expansion and gapless gutter hitboxes still work, tablet/mobile grids have no horizontal overflow, reduced motion keeps widths static, and no runtime errors fire.
- 2026-07-08: Removed the sponsor gallery image zoom-on-hover so every card behaves like a widening crop reveal instead of scaling the underlying image. Ran `npm run build`; headless Brave CDP verified all six sponsor images keep the same computed transform before/after hover, each panel still expands to the selected width, hover filter treatment still responds, reduced motion stays static, and no runtime errors fire.
- 2026-07-08: Corrected the sponsor section back to the RMR design system by removing the decorative dot, returning sponsor heading/lead sizing to documented h2/body scales, and stabilizing sponsor image crop widths so panel expansion reveals the crop without perceived zoom. Ran `npm run build`; headless Brave CDP verified FAQ -> sponsor -> Waves order, no horizontal overflow at desktop/tablet/mobile, no sponsor dot pseudo content, desktop h2 = 48px and lead = 16px, all six sponsor images keep stable dimensions/transform through hover, panel expansion and gapless gutter handoff still work, reduced motion stays static, and no runtime errors fire.
- 2026-07-08: Replaced sponsor gallery placeholder wordmarks with five uploaded abstract partner logo marks, excluding the unused letter marks and D-lookalike asset, and replaced numeric panel indexes with short institution-role labels plus northeast arrow affordances. Ran `npm run build`; verified selected logo files copy to `dist/Website Images/advertising partner logos/`, excluded files are not referenced, FAQ -> sponsor -> Waves order remains intact, headless Brave CDP passes desktop/tablet/mobile overflow checks, gapless hover handoff, stable background-image crops, logo/label/arrow hover motion, no live outbound links, reduced-motion static behavior, and no runtime errors. Captured and inspected `/private/tmp/rmr-sponsor-partner-gallery.png`.
- 2026-07-08: Added the sixth uploaded partner logo to the final sponsor panel as clean public asset `D.png` and removed the arrow travel so the northeast arrow stays static on hover. Ran `npm run build`; verified all six logo marks copy to `dist/Website Images/advertising partner logos/`, the old text-only sixth card is gone, arrow hover transforms are removed, headless Brave CDP confirms all six panels still expand with stable image crops and static arrows, desktop/tablet/mobile have no overflow, reduced motion is static, and no runtime errors fire. Captured and inspected `/private/tmp/rmr-sponsor-partner-gallery-final.png`.
- 2026-07-09: Corrected the sponsor section body-copy treatment while preserving the approved wording. Reverted the sponsor line to `Relevant companies can support the RMR experience without turning the page into a billboard.`, moved it off the global `.lead` class, styled it at the design-system body scale, mirrored the pattern in `website/pages/styleguide.html`, ran `npm run build`, verified generated home output contains the restored copy and no replacement line, and ran `git diff --check`.
- 2026-07-09: Matched the Featured Sponsors scroll entrance to the Process card reveal without changing sponsor card visuals. Removed the extra gallery fade/blur reveal and changed sponsor panels to the Process-style `y: 36`, opacity fade, `0.1` stagger, `0.7s`, `power3.out` sequence. Ran `npm run build`, verified generated home output contains the Process-style sponsor panel tween and no old sponsor gallery/blur tween, updated the styleguide spec, and ran `git diff --check`.
- 2026-07-09: Fixed disappearing Featured Sponsors cards caused by the sponsor panel entrance tween leaving panels in their hidden state. Kept the Process-style motion values but switched the panel reveal to `fromTo` with `immediateRender: false` and `clearProps: 'opacity,transform'`. Ran `npm run build`, verified source plus generated home outputs use the safe reveal and no fragile `.from()`/blur reveal remains, and ran `git diff --check`.
- 2026-07-09: Removed the sponsor-card radial shimmer and tied Featured Sponsors plus Process cards to a shared `processCardReveal` motion config. Sponsor and Process cards now use the same `fromTo` reveal values (`y: 36`, opacity fade, `0.1` stagger, `0.7s`, `power3.out`) with cleanup to prevent stuck hidden states. Ran `npm run build`, verified source and generated home output use the shared reveal, confirmed old sponsor blur/gallery reveal and radial shimmer are absent, updated the styleguide spec, and ran `git diff --check`.
- 2026-07-09: Corrected the Featured Sponsors entrance mismatch by removing `transform`, `opacity`, and `filter` CSS transitions from the outer sponsor panel hitboxes. Width/height transitions remain for the hover accordion, while GSAP now fully owns the Process-style entrance motion. Ran `npm run build`, verified source and generated home outputs keep outer sponsor panel transitions to width/height only, confirmed shared reveal/no-shimmer checks, and ran `git diff --check`.
- 2026-07-09: Fixed the remaining Featured Sponsors reveal mismatch by explicitly setting sponsor panels to `opacity: 0` and `y: 36` with `gsap.set()` before the ScrollTrigger timeline, then revealing them with the same Process-card `.to()` timing. Added a hash-landing fallback so `#sponsor-ledger` hard refreshes scroll to the anchor and play the same timeline instead of leaving cards hidden. Ran `npm run build`, verified generated home output contains the explicit hidden set, `.to()` reveal, no sponsor `fromTo`, no wrapper/blur reveal, no radial shimmer, and no outer panel opacity/transform/filter CSS transitions. Headless Brave CDP verified top-of-page cards start hidden, scrolling into Featured Sponsors reveals all six to opacity `1` with transform cleared, direct `#sponsor-ledger` landing leaves cards visible, reduced motion keeps cards static/readable, and no runtime errors fired.
- 2026-07-09: Added the homepage cold-entry loading prelude for hard homepage entries only. The loader uses pre-first-paint eligibility, a full-screen warm canvas, large Instrument Serif percentage, transform-scaled progress bar, and a sequenced handoff into the existing dark curtain and hero entrance. Ran `node --check website/scripts/router.js`, `npm run build`, static generated-output checks for loader markup/trigger/sequence/router cleanup, `git diff --check`, and headless Brave CDP checks for direct entry, refresh, same-origin hard entry suppression, reduced-motion suppression, internal soft-navigation suppression, desktop/mobile no-overflow, percentage reaching `100`, hero reveal, and no runtime errors.
- 2026-07-09: Corrected the homepage cold-entry loader to match the supplied reference and RMR section typography instead of hero-scale numerals. Updated the loader and styleguide preview to use Instrument Serif `h2` sizing (`clamp(28px, 4vw, 48px)`, `1.12` line-height, `-0.025em` tracking), centered the composition, shortened the rail to 268px/1px, changed the styleguide sample to `30%`, and lengthened the progress to 1.8s. Ran `npm run build`, verified generated `dist/index.html` and `dist/rmr-home.html` contain the new values and no old oversized loader values, ran `git diff --check`, and used headless Brave CDP at `http://127.0.0.1:4210/` to confirm 48px/53.76px/-1.2px computed type, 268px rail, centered placement, no overflow, clean loader removal, hero reveal, reduced-motion skip, no runtime errors, and captured `/private/tmp/rmr-home-loader-centered.png`.
- 2026-07-09: Fixed the hero load choreography after the cold-entry loader by delaying the autonomous `Your wealth.` / `Your way.` headline text swap until the GSAP hero entrance timeline completes. Replaced the eager page-load `setTimeout(...setInterval...)` with a guarded `heroTextSwap` controller, registered cleanup for soft navigation, skipped the loop in reduced motion, and started it from `heroTl.onComplete` with a short grace delay. Ran `npm run build`, verified generated home outputs use the new `heroTextSwap`/`eventCallback('onComplete')` path and no old eager timer, ran `git diff --check`, and used headless Brave CDP to confirm the headline remains `Your wealth.` during and immediately after hero entrance, swaps to `Your way.` later, and logs no runtime errors.
- 2026-07-09: Tightened the hero headline text-swap timing so `Your way.` arrives shortly after the hero entrance instead of waiting for the first full rotation interval. Updated `heroTextSwap.start()` to run one first `cycleHeroText()` after a 420ms post-entrance delay, then continue on the normal 2.8s interval. Ran `npm run build`, verified generated home outputs include `firstSwapDelay`/`swapInterval`, ran `git diff --check`, and used headless Brave CDP to confirm `Your wealth.` is visible before the first swap, `Your way.` is visible and settled shortly after, the loader is gone, headline opacity is 1, and no runtime errors fire.
- 2026-07-09: Replaced the cold-entry loader's linear-feeling progress tween with staged deterministic progress (`0-18-46-63-78-86-97-100`) driven by one shared progress value for the percentage and transform-scaled bar. Added a page/font readiness gate with a 2.2s cap, kept the stable tabular numeral treatment, and documented the pattern in the styleguide. Ran `npm run build`, verified generated `dist/index.html` and `dist/rmr-home.html` contain the staged loader logic and no old `loaderDuration` tween, ran `git diff --check`, and used headless Brave CDP at `http://127.0.0.1:4211/` to confirm cold-entry samples are monotonic but uneven, the loader pauses near 86 before finishing, removes itself, curtain/hero handoff completes, same-origin and reduced-motion paths skip the loader, desktop/mobile have no overflow, no runtime errors fire, and captured `/private/tmp/rmr-home-loader-staged.png`.
- 2026-07-09: Reverted the cold-entry loader away from staged pseudo-realistic progress and into a calmer screenshot-matched loader face: compact `LOADING` label, two-digit `00%` value, 404px rail, smooth eased 00-100 progress, and a 320ms end hold before the exit/curtain handoff. Ran `npm run build`, verified generated home outputs contain the new loader meta layout and no staged `readinessGate`/`tweenProgress` logic, ran `git diff --check`, and used headless Brave CDP at `http://127.0.0.1:4212/` to confirm the loader starts at `00%`, stays two-digit until `100`, reaches and visibly holds at `100%`, removes cleanly, hero finishes, same-origin and reduced-motion paths skip the loader, desktop/mobile have no overflow, no runtime errors fire, and captured `/private/tmp/rmr-home-loader-smooth-00.png`.
- 2026-07-09: Corrected the cold-entry loader typography and vertical centering to match the RMR eyebrow token. Changed the loader label/value to Inter 11px, weight 500, 0.12em tracking, uppercase, removed the `translateY(5vh)` offset so the loader is truly centered, and mirrored the pattern in the styleguide. Ran `npm run build`, verified generated home outputs contain the 11px/500/0.12em styling and no `translateY(5vh)`, ran `git diff --check`, and used headless Brave CDP at `http://127.0.0.1:4214/` to confirm `00%` start, computed eyebrow styling, `centerDelta: 0`, visible `100%` hold, loader removal, hero completion, reduced-motion/same-origin skips, no overflow, no runtime errors, and captured `/private/tmp/rmr-home-loader-centered-eyebrow.png`.
- 2026-07-09: Completed the image SEO alt-text pass. Added contextual `imageAlt` metadata for all blog posts, replaced remote `picsum.photos` placeholders with owned local article images, switched active image references to descriptive filenames, updated homepage/sponsor/styleguide alt text, generated article-specific OG/Twitter/JSON-LD image metadata, and added image sitemap entries. Ran `npm run build`, `node --check scripts/build-static.mjs`, `xmllint --noout dist/sitemap.xml`, `git diff --check`, static checks for non-empty production image alt text/no `picsum.photos`, article metadata checks, and local HTTP checks for home, insights, an article route, and referenced image assets. Playwright was unavailable in this environment.
- 2026-07-09: Reverted the three bottom Insights list thumbnails (`hidden-fees-401k`, `questions-ask-financial-advisor`, `stock-market-retirement-perspective`) back to their previous seeded `picsum.photos` images after visual review. Removed the three generated replacement images from source assets, rebuilt, verified they are absent from `dist`, confirmed the old image URLs are active in generated blog data, validated `dist/sitemap.xml`, ran `git diff --check`, and opened the rebuilt blog preview locally.
- 2026-07-09: Added a full-color test for the homepage Featured Sponsors gallery by removing the sponsor-only dark gradient overlay and setting sponsor media filters to `none` in both the homepage and styleguide preview. Ran `npm run build`, verified source and generated home outputs no longer include the sponsor grayscale/brightness filter or dark overlay, ran `git diff --check`, opened `/#sponsor-ledger` locally, and confirmed the homepage plus six sponsor media assets return `200`.
- 2026-07-09: Corrected the Featured Sponsors full-color test so full color appears only on hover. Restored the resting dark gradient scrim and muted grayscale/brightness media filter, then added hover-only scrim opacity removal and `filter: none` for sponsor media in both homepage and styleguide CSS. Ran `npm run build`, verified source and generated home outputs contain resting muted CSS plus hover-only full-color CSS, ran `git diff --check`, and reopened `/#sponsor-ledger` locally.
- 2026-07-09: Added a second homepage Featured Sponsors section directly below the current one for side-by-side scrolling comparison. The first section keeps hover-only full color with no card border, while the lower comparison copy restores the older hover overlay/filter treatment without bringing back the removed border. Ran `npm run build`, verified generated `dist/index.html` and `dist/rmr-home.html` include both sponsor sections and the overlay comparison modifier, ran `git diff --check`, and opened the local preview at `http://127.0.0.1:4216/?v=sponsor-compare-20260709#sponsor-ledger`.
- 2026-07-09: Removed the temporary lower Featured Sponsors comparison section and kept the approved hover-only color version. Ran `npm run build`, verified generated home output has one `#sponsor-ledger` section and no overlay comparison selectors, ran `git diff --check`, and reopened the local preview.
- 2026-07-09: Updated the Featured Sponsors partner names to Sora Bank, Oriel Retirement, Axiom Planning, Orbis Trust, Northline Financial, and Dover Partners in the homepage and styleguide preview, with matching sponsor logo alt text. Ran `npm run build`, verified generated home output contains the new names and no old category labels in the sponsor markup, and ran `git diff --check`.
- 2026-07-09: Hid the Featured Sponsors label arrows in the homepage and styleguide preview while leaving sponsor labels, logo marks, hover expansion, and color treatment intact. Ran `npm run build`, verified generated home output includes hidden sponsor arrow CSS and no sponsor arrow hover opacity rule, and ran `git diff --check`.
- 2026-07-09: Changed the sponsor section eyebrow from `Featured Sponsors` to `Trusted Partners` and replaced the body copy with the approved limited-partner collection sentence in the homepage and styleguide preview. Ran `npm run build`, verified generated home output contains the new copy and no old sponsor line, and ran `git diff --check`.
- 2026-07-09: Refined the Trusted Partners section copy to use the approved heading `A small space for companies that support the retirement journey` and body line `A carefully selected group of partners we trust to help people navigate retirement.` in the homepage and styleguide preview. Ran `npm run build`, verified generated home output contains the new text and no prior wording, and ran `git diff --check`.
- 2026-07-09: Updated the Trusted Partners body copy to end with `navigate retirement with confidence.` in the homepage and styleguide preview. Ran `npm run build`, verified generated home output contains the revised sentence and no previous sentence, and ran `git diff --check`.
- 2026-07-09: Set the second Trusted Partners card, Oriel Retirement, to start open by default on hover-capable desktop galleries while normal hover still takes over when the visitor moves through the gallery. Mirrored the default-open state in the styleguide preview, ran `npm run build`, verified generated home output includes the default-open CSS and second-card class, and ran `git diff --check`.
- 2026-07-09: Reverted the Trusted Partners default-open experiment so all partner cards start evenly again and only open on hover. Removed the `is-default-open` class and related CSS from the homepage and styleguide preview, ran `npm run build`, verified generated home output has no default-open selectors while hover color behavior remains, and ran `git diff --check`.
- 2026-07-09: Reintroduced the Trusted Partners default-open state on the first card, Sora Bank, instead of the second. The first card starts expanded/full-color until gallery hover takes over; the styleguide preview mirrors the behavior. Ran `npm run build`, verified generated home output has the default-open class on the first sponsor card only, and ran `git diff --check`.
- 2026-07-09: Lightened the resting Trusted Partners card treatment while preserving the grayscale black-and-white feel. Reduced the resting scrim from `rgba(17,17,16,0.18/0.72)` to `0.1/0.52` and raised resting grayscale brightness from `0.58` to `0.66` in the homepage and styleguide preview. Ran `npm run build`, verified generated home output contains the lighter values and still keeps hover/default-open full color, and ran `git diff --check`.
- 2026-07-09: Moved the Trusted Partners default-open state from the first card to the third card, Axiom Planning, while keeping the lighter grayscale resting treatment. Ran `npm run build`, verified generated home output places `is-default-open` on the Axiom Planning panel and leaves Sora Bank unmarked, and ran `git diff --check`.
- 2026-07-09: Added tablet/mobile tap-to-active behavior for Trusted Partners cards. On coarse/hoverless devices, tapping a card applies the full-color active treatment to that card without expanding grid width, and tapping another card moves the active state. Desktop hover/default-open behavior remains unchanged; the styleguide preview mirrors the touch behavior. Ran `npm run build`, `node --check scripts/build-static.mjs`, verified generated home output includes touch-active CSS/JS plus the lighter resting grayscale treatment, and ran `git diff --check`.

## Backlog

_Add items here as they come up._
